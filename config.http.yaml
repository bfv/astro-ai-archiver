# Example configuration for HTTP transport
# Copy this to config.yaml to use HTTP instead of stdio

scan:
  directory: "C:\\Users\\YourName\\Documents\\Astrophotography"  # Path to your FITS files
  recursive: true                                                # Scan subdirectories
  on_startup: true                                               # Scan on server startup

database:
  path: ""  # Leave empty to use default: {scan.directory}/.aaa.db

transport:
  type: "http"           # Use HTTP transport instead of stdio
  http:
    host: "localhost"    # HTTP server host
    port: 8080           # HTTP server port

logging:
  level: "info"          # debug, info, warn, error
  format: "console"      # console or json