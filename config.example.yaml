# Example configuration for Astro AI Archiver
# Copy this to config.yaml and modify as needed

scan:
  directory: "C:\\Users\\YourName\\Documents\\Astrophotography"  # Path to your FITS files
  recursive: true                                                # Scan subdirectories
  on_startup: true                                               # Scan on server startup
  workers: 4                                                     # Number of parallel workers (default: runtime.NumCPU())

database:
  path: ""  # Leave empty to use default: {scan.directory}/.aaa.db

transport:
  type: "stdio"  # "stdio" or "http"
  http:
    host: "localhost"  # "localhost" = local only, "0.0.0.0" = all interfaces (remote access)
    port: 8080         # Port number for HTTP server

logging:
  level: "info"    # debug, info, warn, error
  format: "console"  # console or json
